import streamlit as st

# -------------------------------------------------
# 1. நூலக இறக்குமதி (Error Handling Import)
# -------------------------------------------------
try:
    import tolkapy
    from tolkapy import vidhikal
    status = True
except ImportError:
    try:
        import tamilrulepy as tolkapy
        from tamilrulepy import vidhikal
        status = True
    except:
        status = False

# -------------------------------------------------
# 2. பக்க வடிவமைப்பு
# -------------------------------------------------
st.set_page_config(page_title="Tolkapy - தொல்காப்பிய ஆய்வு", layout="wide")

st.title("📜 Tolkapy (தொல்காப்பி)")
st.subheader("இலக்கண ஆய்வு: மெய்ம்மயக்கம், மொழிமுதல், இறுதி, புணர்ச்சி")

if not status:
    st.error("❌ நூலகத்தை ஏற்ற முடியவில்லை. 'Manage app' -> 'Reboot App' செய்யவும்.")
    st.stop()

# -------------------------------------------------
# 3. பிரிவுகள் (Tabs)
# -------------------------------------------------
tab1, tab2, tab3, tab4 = st.tabs(["மெய்ம்மயக்கம்", "மொழிமுதல்", "மொழியிறுதி", "புணர்ச்சி"])

# --- TAB 1: மெய்ம்மயக்கம் ---
with tab1:
    st.header("மெய்ம்மயக்கம் ஆய்வு")
    word_m = st.text_input("சொல்லை உள்ளிடவும்:", key="m1", placeholder="எ.கா: தங்கம்")
    if st.button("ஆராய்க", key="b1"):
        if hasattr(vidhikal, 'meymayakkam_checker'):
            res = vidhikal.meymayakkam_checker(word_m)
            st.success(f"ஆய்வு முடிவு: {res}")

# --- TAB 2: மொழிமுதல் (சிக்கல் 1 தீர்வு) ---
with tab2:
    st.header("மொழிமுதல் ஆய்வு")
    word_f = st.text_input("சொல்லை உள்ளிடவும்:", key="f1", placeholder="எ.கா: கற்றல்")
    if st.button("சரிபார்", key="b2"):
        if hasattr(vidhikal, 'mozhi_muthal_checker'):
            res = vidhikal.mozhi_muthal_checker(word_f)
            st.info(f"ஆய்வு முடிவு: {res}")

# --- TAB 3: மொழியிறுதி (சிக்கல் 2 தீர்வு) ---
with tab3:
    st.header("மொழியிறுதி ஆய்வு")
    word_e = st.text_input("சொல்லை உள்ளிடவும்:", key="e1", placeholder="எ.கா: நிலன்")
    if st.button("சரிபார்", key="b3"):
        if hasattr(vidhikal, 'mozhi_iruthi_checker'):
            res = vidhikal.mozhi_iruthi_checker(word_e)
            st.info(f"ஆய்வு முடிவு: {res}")

# --- TAB 4: புணர்ச்சி (சிக்கல் 3 & 4 தீர்வு) ---
with tab4:
    st.header("புணர்ச்சி ஆய்வு")
    c1, c2 = st.columns(2)
    with c1: n_mozhi = st.text_input("நிலைமொழி:", key="n1", placeholder="அப்பா")
    with c2: v_mozhi = st.text_input("வருமொழி:", key="v1", placeholder="இன்")
    
    if st.button("புணர்க்க", key="b4"):
        if n_mozhi and v_mozhi:
            if hasattr(vidhikal, 'punarchi_checker'):
                res = vidhikal.punarchi_checker(n_mozhi, v_mozhi)
                st.success(f"புணர்ச்சி முடிவு: {res}")
            else:
                st.code(f"{n_mozhi} + {v_mozhi}")
                st.write("தகவல்: புணர்ச்சி விதிகள் மேம்படுத்தப்படுகின்றன.")

st.divider()
st.caption("வழங்குபவர்: கணியம் அறக்கட்டளை & Tolkapy குழுவினர்")
