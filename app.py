import streamlit as st

# பக்க வடிவமைப்பு
st.set_page_config(page_title="தொல்காப்பி - மெய்மயக்கம்", page_icon="📜")

# நூலகத்தை இறக்க முயற்சிப்போம்
try:
    import tolkapy
    from tolkapy import vidhikal
    status = True
except ImportError:
    status = False

st.title("📜 தொல்காப்பி (Tolkapy)")
st.subheader("தமிழ் மெய்மயக்கம் விதி சரிபார்ப்பான்")

if status:
    st.success("✅ தொல்காப்பிய விதிகள் தயாராக உள்ளன!")
    
    word = st.text_input("தமிழ் சொல்லை உள்ளிடவும்:", "தமிழ்")

    if st.button("சரிபார்"):
        if word:
            try:
                # நூலகத்தில் உள்ள சரியான சார்பை அழைக்கிறோம்
                result = vidhikal.meymayakkam_checker(word)
                
                if result == True:
                    st.balloons()
                    st.success(f"✅ '{word}' - மெய்மயக்கம் விதிகளின்படி சரியான சொல்.")
                else:
                    st.warning(f"⚠️ ஆய்வு முடிவு: {result}")
            except Exception as e:
                st.error(f"ஆய்வு செய்வதில் பிழை: {e}")
        else:
            st.info("தயவுசெய்து ஒரு சொல்லை உள்ளிடவும்.")
else:
    st.error("❌ நூலகத்தை இணைக்க முடியவில்லை.")
    st.info("தயவுசெய்து 'Manage App' -> 'Reboot App' என்பதைச் செய்யவும்.")

st.divider()
st.caption("வழங்குபவர்: கணியம் அறக்கட்டளை & Tolkapy குழுவினர்")
